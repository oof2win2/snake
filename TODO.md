#  TODO LIST
- code function `getAreaSize()` , which returns the size of the map/game area in a data type of a structure and removes the stop. The stop is saved as the reserved variable `END` (set 14. 7) - for example like this:
        struct area_size() {
        int w;
        int h;
        }
        area_size.w
        area_size.h
